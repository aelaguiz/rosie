# OpenAI API Configuration
OPENAI_API_KEY=sk-proj-...

# LLM Models
# For thought detection (default: gpt-4o-mini)
THOUGHT_DETECTION_MODEL=gpt-4o-mini

# For voice command detection (default: gpt-4o)
VOICE_COMMAND_MODEL=gpt-4o

# Whisper Speech-to-Text Models
# Options: tiny, base, small, medium, large-v1, large-v2, large-v3
# For initial/offline model (default: tiny)
WHISPER_MODEL=tiny

# For realtime transcription model (default: same as WHISPER_MODEL)
WHISPER_REALTIME_MODEL=tiny

# Knowledge Storage Backend
# Options: graphiti (default), mock (for testing)
KNOWLEDGE_BACKEND=graphiti

# Neo4j Configuration for Graphiti Knowledge Graph
# Default values for local Docker development
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=rosie_local_password